# rewire-base

Shared base utilities for the ReWire compiler: source annotations
(`ReWire.Annotation`), bit vectors, the `Config`/`Flags` records, diagnostics
(`ReWire.Error`), pretty-printing, and the SYB/orphan-instance glue. The
foundation the other ReWire packages build on.
